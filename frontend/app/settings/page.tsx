import SettingsClient from "../../components/settings-client";

export const metadata = {
  title: "Настройки интеграций — Stat Funnel",
};

export default function SettingsPage() {
  return <SettingsClient />;
}
